name: Python CI

on: push

jobs:
  test:

    runs-on: self-hosted
    strategy:
      matrix:
        python-version: [2.7]

    steps:
    - uses: actions/checkout@v2
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v2
      with:
        python-version: ${{ matrix.python-version }}
    - name: Install dependencies
      env:
        PIP_INDEX_URL: https://pypi.pacificclimate.org/simple
        CPLUS_INCLUDE_PATH: /usr/include/gdal
        C_INCLUDE_PATH: /usr/include/gdal
      run: |
        sudo apt-get update
        sudo apt-get install -y gcc libhdf5-dev libnetcdf-dev libgdal-dev
        pip install -U pip pytest
        pip install numpy
        pip install sphinx gdal==3.0.4
        pip install $(sed '/GDAL==2.2/d' requirements.txt) -r test_requirements.txt
        pip install .
    - name: Test with pytest
      env:
        DSN: ${{ secrets.DSN_PCIC_META_TEST }}
      run: |
        py.test -m "not local_only" -v --tb=short
